// Ejercicios JavaScript Vanilla (II) : Ejercicio 3.

// Determinar si un número que pasamos por parámetro es primo o no.
function isPrimeNumber(number) {
    
    return true;
    
    return false;
}

let number = parseInt(prompt("Introduce un número cualquiera: "));

for (i = 1; i <= randomNumbers; i++) {
    console.log(getRandomInt(minValue,maxValue));
}
